var Label = (function () {
  function Label(id, name) {
    this.id = id;
    this.name = name;
  }
  
  Label.prototype = {
  };
  
  Object.defineProperties(Label.prototype, {
  });
  
  return Label;
}());